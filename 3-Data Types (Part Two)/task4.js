let points = 10;

// points = ++points 
// points = ++points 
// points = ++points 

points += 3 

console.log(points); // 13

// points= --points
// points= --points
// points= --points
// points= --points
// points= --points

points -= 5

console.log(points); // 8;